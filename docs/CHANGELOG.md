# Changelog

## 2026-08-06
- **Nathan's Territory Planning /molsoncoors** renamed from Fair Share, with an animated peak mark adapted from the Coors Banquet artwork replacing the FS tile. Order desk narrowed to one lane, a store, defaulting to 99 Ranch Market Rowland Heights; Harbor's sell-in stays on /plan and /distributor. Every store email now addresses the Store Manager. The email itself rebuilt on measured evidence rather than taste: approval is one word, the ask sits in the subject and first two lines, the item list moved below it, the deadline is a real load cutoff, and the close de-risks instead of urging. Fixed the velocity anchor, which was 26 cases a week for one SKU in one store and produced a 136-case order for a single supermarket; now 11, and 99 Ranch reads 42. Send moved to the bottom of the compose pane after a grid row sized auto let it grow out of view. OpenStreetMap and CARTO credit collapsed behind a circled i rather than removed, which the licences require.
- **Fair Share /molsoncoors** package glyphs. Six drawn vessels (closed carton, open carrier with the necks showing, tall can, stub can, stubby, keg) adapted from the Coors Banquet illustrations, coloured by brand family from the same vocabulary the chips and the mix bar use, on the order card, the running order, the store portal and the portfolio package tables. Drawing them exposed a modelling error: `unitsPerCase` is the shipper, not the pack, so a 19.2oz single was rendering as a twelve pack. `PackageFormat` now carries `packUnits` and the glyph selects on the selling unit. Demo Mode badge replaced with a two-click reset that clears the plan and returns every screen to its opening state; `RESET_ALL` bumps a nonce so screens holding local state, like the order desk's quantity diff, clear themselves. Footer disclaimer and methodology link out of the chrome, Leaflet attribution off the map.

## 2026-08-04
- **Ono /ono** dashboard pass ten. Paid flight hard-capped at three months with modeled ad fatigue: a geo-radius buy hits the same local audience repeatedly, so effective CPC rises about 16% per additional month and entry conversion softens with it. The flight-length slider maxes at 3 and reads capped, and the media panel turns amber at the cap showing month-three click cost against month one. Added scale saturation so wider rollouts are less efficient not more: at 36 stores clicks run +13% and entry conversion -6% against a 12-store buy, indexed across the store levels. Regional scale-up rebased with saturation so its return lands near 6x (was 10x from linear scaling), and incremental revenue per store per month now reads $367 / $774 / $925 across the three presets, which is the honest shape where a bigger rollout is only modestly better per location. Fixed three leading-comma copy artifacts in the media panel.
- **Ono /ono** dashboard pass nine. Conservative floor is now the default view and the reset target. Regional scale-up rebased to same-proven-conversion-wider-footprint after its six optimistic assumptions were compounding to 31x conservative revenue; now 10.8x. Added incrementality: four sliders for the share of gift-card, repeat, delivery and catering value that is genuinely new rather than business that would have walked in anyway, with a True incremental contribution card and incremental figures on every preset. Fixed two stale single-flight metrics that contradicted the programme model on screen: the risk register compared whole-programme media to launch-one buyers only (reading $307 CAC against a $58 sticky bar) and the break-even panel said contribution never turns positive while the programme turned positive at launch 2. Also fixed revenue-per-store-per-month, which referenced the programme window before it was assigned.
- **Ono /ono** dashboard passes seven and eight. Ads now run one short flight (2 months default) with a lever for how many launches carry paid media, so media is $24k at 12 stores and every relaunch after markets to the owned list free. Sticky bar shows the monthly burn so spend arithmetic is checkable, change pills moved beside their values instead of over the labels, and the window label now reads ads run for N months then stop, revenue tracked N months as cards get redeemed. Added Uber Eats delivery (net of marketplace commission), catering with a game-day calendar lift, and referral virality with a viral coefficient, all broken out in a Where the money comes from panel. Programme CAC now counts buyers from the free relaunches ($40 per buyer, $2 per contact, was misreading $177), and incremental revenue per store per month grounds the ROAS multiple. Generic pulsing status dots replaced with hand-drawn animated Ono markers: a BBQ grill flame for live and urgent, a plumeria for status.
- **Ono Hawaiian BBQ** added as a Marketing Manager work sample at `/ono`: three-view funnel (Enter / Convert / Measure). Enter: $200 gift-card sweepstakes with A/B prize structures (equal split vs $400/$150/$50 tiered, toggle in nav), animated three-act raffle scene, one-question-per-screen capture, recruiter prefill. Convert: menu match with Uber Eats delivery scene, $30 code toward gift-card purchase, referral cap 3, group-order calculator linking to Ono's real catering page, and a game-day events rail (Kings, Galaxy, LAFC, WWE Raw, UFC 331 — verified fall 2026 dates) with animated viewer modals. Measure: prepaid growth intelligence dashboard — 30+ draggable assumptions, derived radius media, prize economics at $800/mo, deferred-revenue accounting, sensitivity, break-even, risk register, guided demo, CSV export. Same-day follow-up: A/B switch moved into the nav as a sliding control, tiered $400/$150/$50 values on the card designs in variant B, group-size slider to 12 with falling per-plate pricing, sticky game-day floater, animated share CTA, and a site-wide spatula cursor with egg-flip clicks. Second pass: lifecycle section reframed as a customer journey with Welcome / Spotlight / Reminder / Personalized offer / Win-back stages, and the group calculator now shows the order paid from the $230 gift-card stack, $200 card plus $30 bonus, with a balance bar and plates-left-after-tonight math. Third pass: gift-card payment is now an opt-in toggle showing the stacked saving, at 12 people $67.80 group rate plus $16.98 card bonus equals $84.78 total, with an effective per-plate price and a Buy the $200 gift card CTA into Ono shop; the offer-card buy button reads Buy the card, add my $30 on one line. Fourth pass: how-it-works section reframed from what the business keeps to Every entry creates three business assets: a future customer, consumer insights, measurable revenue. Fifth pass, dashboard as campaign manager: default preset is a 12-Store Test at $1,000/store/month with realistic conversion ($1.30 CPC, 30% entry conversion, 3.2% card conversion), saved-view preset cards with a filter-chip row that jumps to each control, a sticky live-metrics bar (cash, contribution, cash ROAS, CAC, cost per lead, CTR) with change deltas and hover glossary, and a Meta ads view (impressions at set CPM, derived CTR, cost per lead, cost per purchase, cash ROAS) in the media panel. Sixth pass, programme economics: the dashboard modelled one flight charging the whole media budget to a single cohort, which made the conservative case negative. Added a launch-cadence model where each launch also markets to the opted-in list already built, at no media cost, with returning contacts credited only their incremental card purchase (no double counting of repeat or catering) and 18% re-engagement decay per launch. Conservative now turns positive at launch 2 and reaches $50k across 4 launches. Saved-view preset cards now show the four assumptions that differ plus each views live programme outcome, and the sticky metrics bar was rebuilt as a spend-generates-revenue narrative (ad spend and window, then new revenue, contribution, return, CAC) with programme-wide figures and glossary hovers. Seventh pass: ads now run one short flight only. Campaign length defaults to 2 months and a new lever sets how many launches carry paid media (default 1), so the media bill is $24k at 12 stores ($12k a month for 2 months) and every relaunch after the flight markets to the owned list at zero media cost. Sticky bar shows the monthly burn rate so the spend arithmetic is checkable on screen, and change pills now flow beside their value instead of covering the label.
- **LA Galaxy** confirmation-page pass. Buy/upsell is now a single season seat from $833 (the prize stays the two-seat pair). Winners floater draw date now derives from the same next-Sunday computation as the hero countdown, so they cannot drift (was hard-coded Aug 20 vs hero Aug 9). Referral reworked to one friend, both double their entries: dropped the Galaxy scarf, free-ticket tiers, and the two-friend ladder; share buttons and link featured; added a visible gold "Copied" state and a high-contrast gold toast (was dark-on-dark). Gift-a-ticket modal is now a purchase-verification flow: name plus season-ticket number, then a glitter-pop celebration on a custom animated LA Galaxy ticket (spinning soccer ball) confirming the friend's free single-match ticket is on the way, redeemable any home match this season, no expiration. Also fixed ava/lex store cross-nav to absolute /galaxy paths (commit 62a8b9b).

## 2026-08-03
- Added **LA Galaxy** as a Paid Media work sample at `/galaxy`: a season-ticket sweepstakes funnel plus two creator stores (`/galaxy/ava` "Ava's Matchday Edit", code AVA10; `/galaxy/lex` "The Section with Lex", code LEX10). Reskin of the pilates funnel with LA Galaxy navy/gold and the real crest, dark theme default, four matchday videos wired. Prize is the prime West Sideline pair: two 2027-28 memberships, $4,516 value, the $2,258 seats, one for you and one for a friend; dropped the "One winner, two seats" line. Buy path is "skip the drawing, from $833", reconciled to live AXS Standard pricing $833 to $2,258. Value calculator rebuilt to compare only single West Sideline tickets vs the West Sideline season using a researched approx $120/match single estimate, with Best value flipping to the season at the approx 19-match crossover. Explorer tab-number contrast fixed to navy on gold; lock-in block now leads with seat certainty.

## 2026-07-20
- Added **Boba Night** as project No. III with a "Midnight Pour" dessert reveal: the cup fills with brown-sugar tea, colorful tapioca pearls stack into a pile, a tinted lid drops on, then a candy-striped straw punches in. Synthesized open sequence (swell, pour, pearl plops, lid thunk, straw pop, sparkle). Copy points to finding good boba in a city you're in.
- Gave **CoverCapy** its own "coverage concierge" reveal (`theme:'cc'`): a clean cyan high-tech slate with a tech grid, the shield mark drawing itself, a scan line sweep, and a verified badge with a pulse ring. Rewrote the copy to the verification angle — verify your insurance with a dentist so you find the right one without guesswork. "Clearer coverage is better care."
- Reworked the shared project modal for clearer separation between sections: a small accent divider marks where the intro ends and the features begin, the selected feature's description sits in its own quiet panel tied to the chips, spacing groups each zone, and the CTA is set apart.
- Made the period after "J" in the top nav pink (with glow), matching the hero.
- Architecture: the baroque salon, boba, and cc reveals all run off one shared modal via a per-project `theme` flag (theme-as-data), so a fourth is trivial.

## 2026-07-18
- Shipped v80 to production (commit d52f4f3): Agent NS interactive "core brain" (press-and-hold NS seal → charge + burst + spoken affirmation), 40 sayings across 4 hold-duration tiers, tap-to-play element sounds with PDF opening in a new tab, luxury gallery theme-toggle chimes, mobile layout polish, clickable email + phone (mailto/tel), and the finalized pink-strike NS favicons.
- 2026-07-20 - /davidjoseph published: full Ritual->Mira reskin (fictional prenatal brand) as a David Joseph Growth Lead work sample; new davidjoseph/ folder (funnel + Partner Circle + 3 creator stores) with 9 original SVGs; all ritual.com deps removed; OG/canonical/favicon added.

## 2026-07-23
- Portfolio dock v2: "Personal Portfolio" tab, two-line chips with Est. dates, chips in a scroll lane with the NS mark and Get in touch pinned so the CTA can't clip, safe-area insets.
- BoxVacay shipped as project four with the recovered "Sundown Round" theme (kick-pad strike scene, strike audio, Explore Camps CTA) — ported from an uncommitted 2026-07-20 preview found in Downloads.
- Modal plates now read "Est. <month year>" only; Ask Agent NS button clears the dock lane under 1080px; salon tassels hidden on phones (blurb overlap); light-mode chip contrast and Boba icon color fixed.

## 2026-07-30
- Added **Wrestle Lore** (wrestlelore.com) as project five with a "Title Night" championship reveal (`theme:'wl'`): arena spotlights sweep in, a hand-drawn vector championship belt drops and settles (crowned center plate, deco sunburst, faceted star, ruby gem links, riveted rim, engraved banner, hex side plates on a long stitched strap with snap studs), five rating stars pop in one by one, a glint sweeps the plate, gold sparks fly, and the ring ropes draw in. Synthesized ring-bell open sequence (crowd swell, three bell strikes, brass fanfare), Web Audio only.
- Copy sells discovery and the live feed: "Your next favorite match is already out there," the connected-archive blurb, pills for Match Discovery / The Lore Feed / Star Ratings / Go Deeper, closing line "Relive the classics. Discover new favorites."
- Dock: fifth chip with a gold belt icon, Est. July 2026, hooked into the shared theme-as-data modal.
- Belt v2 after review: red enamel plate fields (center + hexes), center art swapped from crown/star/sunburst to a gold globe with graticule flanked by laurel branches and a ruby bezel crest set into the rim (original art in the classic international-title spirit). Modal blurb shortened to two paragraphs (white-space pre-line), redundancy cut.
- Modal height fix: blurb trimmed to two short beats, top padding tightened, ending line moved into the Match Discovery detail panel so the CTA stays above the fold on shorter screens.
- Wrestle Lore tag now "The ultimate fan site to discover wrestling". BoxVacay marked under construction: trust line removed, CTA greyed to Coming soon with navigation disabled.
2026-08-08  olesmoky: replaced the loyalty model view with the CRM console (7 tabs: Overview, Media, Enter, Convert, Members, Audience, Plan) rendered in a shadow root behind The model nav button. Inputs reduced to market and spend only; CPM and all rates are labeled constants. Added Something else to step 1 (shares now sum to 100), fixed distSku and store search fallbacks for pick-only. Line chart with break even rule, sticky rail, journeys partition, age bands, 90 day plan. New docs: crm-dashboard-patterns, dashboard-craft-rules, crm-metric-dictionary, olesmoky-evidence-register. Working tree only, not committed.
2026-08-09  olesmoky v111 to v118. Account surface rebuilt as the Jar Passport (five drawn stamps, an unearned one is a dashed outline of the mark you would get; selection is 56px cards not pills; four sounds that exist nowhere else on the site: ink, leaf, stamp press, desk bell on the fifth). Profile tab now reads the record back with what each line changes; Passport tab does the asking. Trade tab added for the SVP Sales case (receipts to account and SKU, 27 CFR 6.3 and 6.96(b)). Model Overview rebuilt in place, not redesigned: seven stage Meta funnel, engagement, four channels, economics, paid ROAS split from CRM programme ROI, seven SHOW THE MATH disclosures. Records renamed to identified members throughout. Market variation engine grounded in drive time, tourism, income and DMA per county plus ten states; three audience segments where the cost ranking is the exact reverse of the new household ranking. Hover glossary on 20 metrics. Shopify Checkout mock for the $9.99 shipping path, built to Shopify's shipped stylesheet tokens. Spend ladders resized: county 500/750/1000, state 3k/10k/15k. Four scenario cards became three. Fixed: MKT carried no name so no market ever varied and the v108 ticket weighting had never fired; .cbar.min re-applied a hard black 1px ring that beat the hidden-state rule on source order (ninth duplicate-selector bug); distance was counted twice in the tour rate giving 28% bookings and 18x returns; real newlines inside a JS-string stylesheet killed every script twice; esc() printed glossary markup as visible HTML; the coupon SVG still said $15 OFF $40 while the model ran on $60. 501 assertions, 19 suites, all passing. New docs: meta-funnel-benchmarks, market-variation-research, account-profile-ux, shopify-integration, interview-coaching, session-2026-08-09-olesmoky-model-checkout.
